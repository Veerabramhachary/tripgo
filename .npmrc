# Match local npm (global legacy-peer-deps=true). EAS runs npm ci
# without that setting unless it is committed here, which causes
# "package.json and package-lock.json are not in sync".
legacy-peer-deps=true